Here's what <%= group.name %> has done recently.

This report was sent on <%= report_localtime.strftime('%A, %B %-d, %Y at %I:%M%P') %>.

<% users.each do |user| %>
<%= user[:user][:username] %>
  <% user[:types].keep_if{|t| t[:entries].count > 0}.each do |type| %>
  <%= type[:title] %>
  <% type[:entries].each do |entry| %>
   * <%= entry.message %>
  <% end %>

<% end %>
<% end %>

<% global.each do |type| %>
<% if type[:entries].count > 0 %>
<%= type[:title] %>
<% type[:entries].each do |entry| %>
   * <%= entry.message %> (via <%= entry.user.username %>)
<% end %>
<% end %>
<% end %>

<% if help_text %>
Did you know?

<%= help_text %>
<% end %>
